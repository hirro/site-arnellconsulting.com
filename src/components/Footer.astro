---
const currentYear = new Date().getFullYear();
---
<footer class="footer">
  <div class="container footer__inner">
    <div class="footer__top">
      <div class="footer__brand">
        <span class="footer__name">Arnell Consulting AB</span>
        <p class="footer__tagline">Technology & Business Consulting</p>
      </div>
      <nav class="footer__links" aria-label="Footer">
        <a href="/#services">Services</a>
        <a href="/#team">Team</a>
        <a href="/#contact">Contact</a>
        <a href="https://www.linkedin.com/in/jimarnell/" target="_blank" rel="noopener noreferrer">LinkedIn</a>
      </nav>
    </div>
    <hr class="divider" />
    <div class="footer__bottom">
      <p class="footer__copy">
        &copy; {currentYear} Arnell Consulting AB. Stockholm, Sweden.
      </p>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--color-dark);
    color: var(--color-dark-text);
    margin-top: auto;
  }

  .footer__inner {
    padding-block: var(--space-3xl);
  }

  .footer__top {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: var(--space-2xl);
    padding-bottom: var(--space-2xl);
    flex-wrap: wrap;
  }

  .footer__name {
    font-family: var(--font-heading);
    font-size: var(--text-lg);
    font-weight: 600;
    letter-spacing: -0.02em;
  }

  .footer__tagline {
    margin: var(--space-xs) 0 0;
    font-size: var(--text-sm);
    color: var(--color-dark-muted);
  }

  .footer__links {
    display: flex;
    gap: var(--space-xl);
    flex-wrap: wrap;
  }

  .footer__links a {
    font-size: var(--text-sm);
    color: var(--color-dark-muted);
    transition: color 0.2s ease;
  }

  .footer__links a:hover {
    color: var(--color-dark-text);
  }

  .footer .divider {
    border-color: rgba(255, 255, 255, 0.12);
  }

  .footer__bottom {
    padding-top: var(--space-lg);
  }

  .footer__copy {
    margin: 0;
    font-size: var(--text-xs);
    color: var(--color-dark-muted);
  }

  @media (max-width: 640px) {
    .footer__top {
      flex-direction: column;
      gap: var(--space-xl);
    }

    .footer__links {
      flex-direction: column;
      gap: var(--space-md);
    }
  }
</style>
